# 运行时生成的悄悄话房间数据（含私密聊天内容，切勿提交）
data/

# 本地环境变量（可能含 API Key）
.env

# 依赖
node_modules/

# Capacitor 生成的 web 资源目录（CI 重新生成）
www/

# Android 构建产物（工程本身提交，产物忽略）
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/captures/

# Electron 打包产物（历史，保留忽略）
release/

# 系统/编辑器
.DS_Store
Thumbs.db
